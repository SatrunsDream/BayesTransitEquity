# `artifacts/tables/`

| Subfolder | Contents |
|-----------|----------|
| **`eda/`** | Exploratory notebooks under `notebooks/eda/` — files named `eda__*__<date>.csv` |
| **`pipeline/`** | Pipeline notebooks `01`–`05` — files named `pipeline__*__<date>.csv` (and related `.json`) |

Figures stay in `artifacts/figures/` (same filenames; no `eda/` vs `pipeline/` split there for now).
