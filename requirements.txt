# --- NumPy / binary extension mismatches (very common in Anaconda "base") ---
# Symptom: "compiled using NumPy 1.x cannot be run in NumPy 2.x" when importing pandas.
# Cause: NumPy was upgraded but Bottleneck, matplotlib C extensions, etc. are still old wheels.
#
# Best fix: use a dedicated env (do not fight `base`):
#   conda env create -f environment.yml
#   conda activate bayestransit
#
# If you must stay on pip in `base` with NumPy 2.x: close Jupyter first, then reinstall *all*
# compiled deps in one go, e.g.:
#   pip install --upgrade --force-reinstall numpy pandas pyarrow scipy Bottleneck matplotlib
#
# If you pin NumPy 1.x (`numpy>=1.26,<2`), recent PyMC/PyTensor may refuse to import — prefer conda-forge env above.

# Core geospatial
geopandas
pyproj
shapely
osmnx

# Transit / accessibility
r5py>=1.1.0,<2

# Census data
censusdatadownloader

# Bayesian modeling
pymc
arviz                  # MCMC diagnostics + visualization
libpysal               # Queen contiguity weights for BYM2 adjacency

# Data
pandas
# Prefer NumPy 2.x + matching wheels (see environment.yml). Pin <2 only if you use an older PyMC stack.
numpy>=1.26
pyarrow                # Parquet support
fastparquet
netcdf4                # InferenceData.to_netcdf() checkpoints (notebook 04)

# Metrics
scipy                  # wasserstein_distance + stats

# Visualization
matplotlib
seaborn
contextily             # basemap tiles for static maps

# HTTP (download_data.py)
requests

# Utilities
pyyaml
tqdm
loguru

# Dev / testing
pytest
jupyter
ipykernel
nbconvert              # execute notebooks (e.g. CI / batch EDA export)

# Optional / Phase 2
# numpyro              # JAX-based sampler for CA/national scale
# jax
# gtfs-realtime-bindings  # GTFS-RT support
